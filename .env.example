# Environment Variables (NEVER commit actual values to git)
# Copy this file to .env.local and fill in your values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# IndexNow (Bing) - Optional
# If you want automated submissions, set these in Vercel env vars.
# INDEXNOW_KEY is also used as the key file name: https://<host>/<INDEXNOW_KEY>.txt
INDEXNOW_KEY=
INDEXNOW_HOST=resibilis.vercel.app
